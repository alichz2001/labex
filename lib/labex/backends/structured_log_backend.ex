defmodule Labex.StructuredLogBackend do

  def init(opts) do
    #open multiple file per struct
  end

  def parse do

  end
  def log() do

  end


  GLogger.follow("profile_id", "streamer_id")
  GLogger.unfollow("profile_id", "streamer_id")

end
